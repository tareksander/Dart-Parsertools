name: example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  parser_builder2_runtime: any

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  parser_builder2: ^0.1.0
  build_runner: ^2.0.0


dependency_overrides:
  parser_builder2_runtime:
    path:
      ../../parser_builder2_runtime
  parser_builder2:
    path:
      ..

